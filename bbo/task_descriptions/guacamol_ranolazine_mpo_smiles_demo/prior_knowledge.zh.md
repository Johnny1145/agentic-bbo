# 领域先验

这是 GuacaMol 中与药物相关的 multi-property objective benchmark 之一。benchmark 定义的参考分子是 ranolazine。

GuacaMol 在原始 benchmark 描述中指出，Ranolazine MPO benchmark 使用只包含一个分子 ranolazine 的 start population。scoring function 将 ranolazine similarity 与 logP、TPSA 和 fluorine-count 项组合起来。

benchmark 不指定额外的结构编辑规则。
