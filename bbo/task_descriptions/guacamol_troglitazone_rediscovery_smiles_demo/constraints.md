# Constraints

Submit one SMILES string in the `smiles` field. Invalid or empty SMILES are accepted by the evaluator but receive score `0.0` and loss `1.0`. The schema default is not an optimizer seed or initial population.
