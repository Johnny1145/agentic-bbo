# 领域先验

这是一个 GuacaMol median-molecule benchmark。该 benchmark 评估模型能否在指定相似度度量下生成位于两个目标结构之间的分子。

对该任务，benchmark 定义的目标分子是 tadalafil 和 sildenafil。scoring function 使用 ECFP6 相似度和几何平均。

benchmark 不指定额外的结构编辑规则。
