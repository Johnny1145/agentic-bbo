# 目标

目标是最小化 evaluator 返回的标量目标 y。

- 决策变量 x：长度为 `2 * n_macro` 的连续向量。
  - 前 `n_macro` 个条目是宏单元的候选 x 坐标。
  - 后 `n_macro` 个条目是宏单元的候选 y 坐标。
- 目标 y：经过 MGO 解码后得到的合法宏单元布局的 HPWL 值。
- 优化方向：y 越小越好。
- 一次评估：提交一个候选向量 x，并接收一个标量 y。
